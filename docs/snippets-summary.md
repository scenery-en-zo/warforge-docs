---
id: snippets-summary
title: All Snippets
sidebar_label: 'All Snippets'
---

# All Snippets Summary

This page lists all available snippets in the Warforge theme with brief descriptions.

## Component Snippets

### Layout & Structure

| Snippet | Description | Usage Example |
|---------|-------------|---------------|
| `section-header` | Header section with logo, nav, cart | `{%!-- Liquid render example --%}'section-header' %}` |
| `section-footer` | Footer section with links and newsletter | `{%!-- Liquid render example --%}'section-footer' %}` |
| `section-hero` | Hero section with WebGL effects | `
<!-- Liquid example -->'section-hero', type: 'webgl' %}```` |
| `section-features` | Features grid (shipping, quality) | `{%!-- Liquid render example --%}'section-features' %}` |
| `section-newsletter` | Newsletter signup form | `{%!-- Liquid render example --%}'section-newsletter' %}` |

### Product Components

| Snippet | Description | Usage Example |
|---------|-------------|---------------|
| `product-card` | Individual product grid item | `
<!-- Liquid example -->'product-card', product: product %}```` |
| `product-image-gallery` | Image gallery with zoom | `{%!-- Liquid render example --%}'product-image-gallery' %}` |
| `variant-selector` | Size/color variant selector | `
<!-- Liquid example -->'variant-selector', options: options %}```` |
| `quantity-selector` | Quantity input for cart | `{%!-- Liquid render example --%}'quantity-selector' %}` |
| `add-to-cart-button` | Add to cart with AJAX | `{%!-- Liquid render example --%}'add-to-cart-button' %}` |

### UI Components

| Snippet | Description | Usage Example |
|---------|-------------|---------------|
| `button` | Button component (primary, secondary) | `
<!-- Liquid example -->'button', label: 'Shop Now' %}```` |
| `badge` | Badge with variant colors | `
<!-- Liquid example -->'badge', text: 'New' %}```` |
| `icon` | SVG icon component | `
<!-- Liquid example -->'icon', name: 'cart' %}```` |
| `input` | Input field with label | `
<!-- Liquid example -->'input', name: 'email' %}```` |
| `textarea` | Textarea with label | `
<!-- Liquid example -->'textarea', name: 'message' %}```` |
| `checkbox` | Checkbox with label | `
<!-- Liquid example -->'checkbox', name: 'subscribe' %}```` |
| `radio` | Radio button group | `
<!-- Liquid example -->'radio', name: 'size' %}```` |

### Feedback Components

| Snippet | Description | Usage Example |
|---------|-------------|---------------|
| `alert` | Alert message (info, success, warning) | `
<!-- Liquid example -->'alert', type: 'success' %}```` |
| `toast` | Toast notification | `
<!-- Liquid example -->'toast', type: 'success' %}```` |
| `cookie-banner` | Cookie consent modal | `{%!-- Liquid render example --%}'cookie-banner' %}` |

### Navigation Components

| Snippet | Description | Usage Example |
|---------|-------------|---------------|
| `breadcrumbs` | Breadcrumb navigation | `{%!-- Liquid render example --%}'breadcrumbs' %}` |
| `pagination` | Product grid pagination | `{%!-- Liquid render example --%}'pagination' %}` |
| `sort-dropdown` | Sort products dropdown | `{%!-- Liquid render example --%}'sort-dropdown' %}` |
| `filter-checkbox` | Checkbox filter item | `
<!-- Liquid example -->'filter-checkbox', name: 'category' %}```` |

### Modal Components

| Snippet | Description | Usage Example |
|---------|-------------|---------------|
| `cart-drawer` | Slide-out shopping cart | `{%!-- Liquid render example --%}'cart-drawer' %}` |
| `quick-view-modal` | Product quick view modal | `
<!-- Liquid example -->'quick-view-modal', product_id: id %}```` |
| `login-modal` | Login form modal | `{%!-- Liquid render example --%}'login-modal' %}` |
| `share-modal` | Share dialog | `{%!-- Liquid render example --%}'share-modal' %}` |

## Collections Snippets

### Product Collections

| Snippet | Description | Usage Example |
|---------|-------------|---------------|
| `collection-header` | Collection title and description | `
<!-- Liquid example -->'collection-header', collection: collection %}```` |
| `collection-filter-toolbar` | Horizontal filter toolbar | `{%!-- Liquid render example --%}'collection-filter-toolbar' %}` |
| `collection-footer` | Pagination footer | `{%!-- Liquid render example --%}'collection-footer' %}` |

### Product Details

| Snippet | Description | Usage Example |
|---------|-------------|---------------|
| `product-title-price` | Product title and price | `
<!-- Liquid example -->'product-title-price', product: product %}```` |
| `product-description` | Product description text | `{%!-- Liquid render example --%}'product-description' %}` |
| `reviews-section` | Product reviews display | `
<!-- Liquid example -->'reviews-section', review_count: count %}```` |

### Related Products

| Snippet | Description | Usage Example |
|---------|-------------|---------------|
| `related-products` | Related products carousel | `{%!-- Liquid render example --%}'related-products' %}` |
| `featured-products-carousel` | Featured products slider | `{%!-- Liquid render example --%}'featured-products-carousel' %}` |
| `new-arrivals-grid` | New arrivals product grid | `{%!-- Liquid render example --%}'new-arrivals-grid' %}` |

## Utility Snippets

### Common Utilities

| Snippet | Description | Usage Example |
|---------|-------------|---------------|
| `metafield-display` | Display metafield value | `
<!-- Liquid example -->'metafield-display', name: 'material' %}```` |
| `price-range-filter` | Price range input | `{%!-- Liquid render example --%}'price-range-filter' %}` |
| `size-guide` | Product size guide popup | `{%!-- Liquid render example --%}'size-guide' %}` |

## Related Documentation

- [Components](../design-system) — All component documentation
- [Sections](./sections) — All section documentation
- [Page Types](./pages-about) — Page type templates

